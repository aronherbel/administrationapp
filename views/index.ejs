<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logout</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0; /* Hintergrundfarbe außerhalb des Containers */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center; /* Container horizontal zentrieren */
            align-items: center; /* Container vertikal zentrieren */
            min-height: 100vh; /* Vollständige Bildschirmhöhe verwenden */
        }

        .container {
            width: 70%;
            text-align: center;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: #333;
        }

        form {
            margin-top: 20px;
        }

        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #0056b3;
        }

        .logout-button {
            position: absolute;
            top: 20px;
            left: 20px;
            font-size: 14px;
            padding: 8px 12px;
        }

        .action-buttons {
            display: flex;
            justify-content: space-around;
            width: 100%;
            margin-top: 20px;
        }

        .action-buttons button {
            width: 45%;
        }
    </style>
</head>
<body>
    <form action="/logout?_method=DELETE" method="POST" class="logout-button">
        <button type="submit">Log Out</button>
    </form>

    <div class="container">
        <h1 id="userName">Hi <%= name %>!</h1>
        <div class="action-buttons">
            <button id="newInvoiceBtn">Neue Rechnung erfassen</button>
            <button id="balanceBtn">Bilanz ausgeben</button>
        </div>
    </div>
</body>
</html>
