<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            position: relative;
        }

        .container {
            width: 70%;
            text-align: center;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: #333;
        }

        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #0056b3;
        }

        .logout-button {
            position: absolute;
            top: 20px;
            left: 20px;
            font-size: 14px;
            padding: 8px 12px;
        }

        .change-name-form {
            margin-top: 20px;
        }

        .change-name-form input[type="text"] {
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            margin-right: 10px;
        }

        .change-name-form button {
            padding: 8px 20px;
        }

        .action-buttons {
            display: flex;
            justify-content: center;
            width: 100%;
            margin-top: 20px;
        }

        .action-buttons form {
            display: flex;
            flex-grow: 1;
            justify-content: center;
        }

        .action-buttons button {
            flex-grow: 1;
            margin: 0 5px;
        }
    </style>
</head>
<body>
    <form class="logout-button" action="/logout?_method=DELETE" method="POST">
        <button type="submit">Log Out</button>
    </form>

    <div class="container">
        <h1 id="userName">Hi <%= name %>!</h1>
        <form class="change-name-form" action="/changeName" method="PATCH">
            <input type="text" name="newName" placeholder="Neuer Name" required>
            <button type="submit">Namen ändern</button>
        </form>          
        <div class="action-buttons">
            <form action="/rechnung" method="GET">
                <button id="newInvoiceBtn">Rechnung erfassen</button>
            </form>
            <form action="/bilanz" method="GET">
                <button id="balanceBtn">Bilanz ausgeben</button>
            </form>
        </div>
    </div>
</body>
</html>
